<div>
    <a href="/dashboard/add/"><button id="add-post-form" class="btn-small">Create post</button></a>
</div>
    {{#if posts.length}}
<div>
    {{#each posts as |post|}}
    {{> post-info post}}
    <a href="/dashboard/edit/{{post.id}}"><button class="btn-up">Edit post</button></a>
</div>
{{/each}}

{{else}}
<h6 class="label">You currently have no posts</h6>

{{/if}}