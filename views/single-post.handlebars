{{> post-info post}}

{{#if loggedIn}}
<form class="mt-3" id="new-comment-form">
  <div>
    <textarea class="w-50 h-200" name="comment-body"></textarea>
  </div>

  <div>
    <button class="btn-save" type="submit">add comment</button>
  </div>
  
</form>
{{/if}}
<div class=" p">
{{> comments post.comments}}</div>

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}